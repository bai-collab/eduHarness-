# instincts — 每次啟動都會全量載入

短小、跨任務、反覆適用的規則放這裡。一條一行，句尾附來源檔名。

**硬上限：本目錄所有檔案合計 200 行（含本檔）。** 這是唯一無條件佔用 context 的知識層，超過就必須合併或淘汰，不得無限增長。

不該放這裡：

- 只適用某類任務的流程 → 寫成 `brain/skills/`。
- 某次失敗的來龍去脈 → 寫成 `brain/errorLog/`，只把結論一行搬過來。

本目錄目前沒有任何規則，請依你自己的使用經驗累積。你新增的檔案不會被 `git pull` 刪除；但本檔屬於內建檔，更新時會被覆蓋，別直接改它。
