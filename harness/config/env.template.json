{
  "schema_version": 1,
  "kind": "local-harness-env",
  "root_mode": "repository-relative",
  "workspace_root": ".",
  "file_overrides": {},
  "path_overrides": {},
  "runtime_overrides": {},
  "notes": [
    "Keep workspace paths relative to the discovered repository root.",
    "Put machine-specific runtime details in env.local.json only.",
    "Use local overrides only for an explicitly approved repository-relative adapter or runtime setting.",
    "Do not store secrets, tokens, cookies or credentials here."
  ]
}